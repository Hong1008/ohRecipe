spring:
  datasource:
    url: jdbc:mysql://35.229.223.80:3306/hong
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        # show_sql: true
        format_sql: true
        use_sql_comments: true
    #database-platform: org.hibernate.dialect.Oracle10gDialect
logging.level:
  org.hibernate.SQL: debug
# org.hibernate.type: trace